<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/css/style.css">
    <title>Dashboard</title>
</head>
<body>
    <h2>Dashboard</h2>
    <ul>
        {{#each posts}}
            <li>
                <h3>{{title}}</h3>
                <p class="blog-post-comment">{{content}}</p>
                <p class="blog-post-user">Posted by: {{userName}} on {{date}}</p>
            </li>
        {{/each}}
    </ul>
</body>
</html>